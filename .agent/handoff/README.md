# Handoff Notes

One file per session/day, filename **and** top-level title both start with the date
(`YYYY-MM-DD-short-slug.md`, `# YYYY-MM-DD — Title`) so entries never get confused with each other
when browsing the folder.

Newest file = most recent handoff. Check it first before resuming work.
